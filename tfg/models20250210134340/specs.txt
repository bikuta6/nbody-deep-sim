model = GraphModel(input_dim=13, # 3 (pos) + 1 (mass) + 3 (vel) + 3 * previous positions
                  node_encoder_dims=[64, 128],
                  edge_encoder_dims=[128],
                  encoder_dropout=0.3,
                  gnn_dim=128,
                  message_passing_steps=2,
                  aggr='sum',
                  output_hiddens=[64, 32],
                  device='cuda',
                  radius=0.5) # also previous steps = 2